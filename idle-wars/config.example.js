// Copie este arquivo para "config.js" e preencha com os dados do SEU projeto Supabase.
// (Settings -> API no painel do Supabase)
//
// Estas chaves são PÚBLICAS (a "anon key" foi feita pra rodar no navegador).
// A segurança do jogo vem do RLS + funções no servidor, não de esconder a chave.
window.IDLE_WARS_CONFIG = {
  SUPABASE_URL: "https://SEU-PROJETO.supabase.co",
  SUPABASE_ANON_KEY: "SUA-ANON-KEY-AQUI",
};
